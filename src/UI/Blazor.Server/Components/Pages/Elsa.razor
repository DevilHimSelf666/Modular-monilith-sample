@page "/elsa"

<h1>Elsa Workflows</h1>
<p>Elsa services are registered. Use the exposed workflow API endpoints to inspect and manage workflows.</p>
<p>Default API root: <code>/workflows</code> (proxied through the hosting app).</p>
